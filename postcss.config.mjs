const config = {
  plugins: {
    '@tailwindcss/postcss': {
      config: './tailwind.config.mjs' // Points to your Tailwind config
    }
  }
};

export default config;

// const config = {
//   plugins: ["@tailwindcss/postcss"],
// };

// export default config;
